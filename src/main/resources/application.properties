spring.application.name=budget_tracker

# Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/budget_tracker
spring.datasource.username=postgres
spring.datasource.password=Kmartinez_30

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# App port
server.port=8080

# JWT Configuration
jwt.secret=MySecretKeyForBudgetTrackerApplicationJwtTokenGenerationAndValidation
jwt.expiration=86400000
